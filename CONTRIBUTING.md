# Contributing

Use a `codex/` branch, Conventional Commits, focused changes, and `npm test` before a pull request. Do not edit generated plugin paths directly; update the importer or ThinkHome integration, then run `npm run build`.

Upstream Caveman improvements belong upstream when generic. ThinkHome marketplace packaging stays downstream.
