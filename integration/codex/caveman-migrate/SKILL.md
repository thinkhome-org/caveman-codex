---
name: caveman-migrate
description: Safely migrate from yibie/caveman-codex. Use when the user asks to migrate the legacy Codex Caveman adaptation.
---

Run `node "$PLUGIN_ROOT/tools/migrate-yibie.mjs"` first. It only reports changes. Run it with `--apply` only after the user explicitly approves its printed plan. Never delete ambiguous files.
